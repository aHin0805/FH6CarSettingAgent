- 所有 API 响应遵循统一的 JSON 结构：包含 `code` (数字状态码), `data` (业务数据或 null), `message` (描述信息)。
- 敏感配置（如 AI API Key）通过 `process.env` 从环境变量获取，并提供默认值或明确的缺失提示。
- 流式响应接口手动设置 SSE 相关 Header（`Content-Type: text/event-stream`, `Cache-Control: no-cache` 等）以确保客户端正确接收流。